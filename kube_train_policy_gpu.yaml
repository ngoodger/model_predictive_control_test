apiVersion: "kubeflow.org/v1alpha1"
kind: "PyTorchJob"
metadata:
  name: "pytorch-mpc-test-policy-gpu-v32"
spec:
  backend: "tcp"
  masterPort: "23456"
  replicaSpecs:
    - replicas: 1
      replicaType: MASTER
      template:
        spec:
          containers:
          - image: gcr.io/proud-spring-222310/mpc_test:9904ec087ab177d4922221ebbd710a547047ad1c
            env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "gs_key.json"
            - name: GCS_BUCKET 
              value: "mpc-test"
            imagePullPolicy: Always
            useHostIPC: true
            name: pytorch
            command: ["python3", "/project/src/main/python/train_policy.py"]
            resources:
              limits:
                nvidia.com/gpu: 1
          restartPolicy: OnFailure
    - replicas: 0
      replicaType: WORKER
      template:
        spec:
          containers:
          - image: gcr.io/proud-spring-222310/mpc_test:9904ec087ab177d4922221ebbd710a547047ad1c
            env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "gs_key.json"
            - name: GCS_BUCKET 
              value: "mpc-test"
            imagePullPolicy: Always
            useHostIPC: true
            name: pytorch
            command: ["python3", "/project/src/main/python/train_policy.py"]
            resources:
              limits:
                nvidia.com/gpu: 1
          restartPolicy: OnFailure

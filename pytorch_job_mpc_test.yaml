apiVersion: "kubeflow.org/v1alpha1"
kind: "PyTorchJob"
metadata:
  name: "pytorch-tcp-dist-mpc-test-v6"
spec:
  backend: "gloo"
  masterPort: "23456"
  replicaSpecs:
    - replicas: 1
      replicaType: MASTER
      template:
        spec:
          containers:
          - image: gcr.io/t-pulsar-217904/mpc_test
            imagePullPolicy: IfNotPresent
            name: pytorch
            resources:
              limits:
                nvidia.com/gpu: 1
          restartPolicy: OnFailure
    - replicas: 1
      replicaType: WORKER
      template:
        spec:
          containers:
          - image: gcr.io/t-pulsar-217904/mpc_test
            imagePullPolicy: IfNotPresent
            name: pytorch
            resources:
              limits:
                nvidia.com/gpu: 1
          restartPolicy: OnFailure
